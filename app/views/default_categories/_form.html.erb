<%= form_for(@default_category) do |f| %>
  <% if @default_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@default_category.errors.count, "error") %> prohibited this default_category from being saved:</h2>

      <ul>
      <% @default_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
